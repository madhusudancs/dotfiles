[user]
	name = Madhusudan.C.S
	email = madhusudancs@gmail.com
[color]
	ui = auto
	branch = auto
	diff = auto
	status = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[alias]
	fw = !git commit -qam 'fix whitespace' && git rebase -q --whitespace=fix HEAD~ && git reset -q HEAD~ 
	ap = !git add -p
	st = status
	sh = !git show -p HEAD
	l = log
	svn-diff = !git-svn-diff
[http]
	cookiefile = /home/madhu/.gitcookies
[push]
	default = simple
[credential]
	helper = gnome-keyring
